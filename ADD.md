# CNKI助手 增量功能说明 by hxy

原版：https://greasyfork.org/users/244539
我只做了部分更新

## 1. 下载目录归档能力增强

- 新增按 `期刊/年份/文件名` 归档下载。
- 后台在 `onDeterminingFilename` 阶段按注册的元信息改写保存路径。
- 新增路径清洗，自动替换 Windows 非法字符，避免命名失败。
- 元信息注册从“仅年份”升级为“年份 + 期刊”。

## 2. 目录匹配稳健性增强（静默阶段也尽量归档）

- 下载前注册支持 `url + aliases` 多键映射。
- 侧边栏会把 `pdfLink` 与 `detailUrl` 一并注册到后台。
- 这样静默阶段在详情页点击 PDF 时，即使下载直链参数变化，也更容易命中同一归档规则。

## 3. 批量下载策略多次迭代后的现状

- 采用串行批量下载（逐篇处理）。
- 单篇成功后等待随机短间隔再继续下一篇。
- 支持从指定论文开始恢复批量（用于验证/恢复场景）。

## 4. 失败恢复主链路

当主下载超时后：

1. 后台静默打开详情页（先不抢前台）。
2. 自动定位并点击 PDF 下载按钮（含轮询与多选择器兜底）。
3. 再等待一段时间监听下载结果。
4. 若静默恢复成功：将该篇视为成功并从列表移除，继续后续下载。
5. 若恢复失败：进入页面类型识别（验证页/重登页/未知页）。

## 5. 验证页与重登页识别分流

- 新增页面识别：
  - 验证页（验证码/滑块等）。
  - 频繁操作需重登页（例如“操作太过频繁，请退出后重新登录”）。
  - 未识别页面。
- 验证页：允许人工处理后恢复下载流程。
- 重登页：按后续策略停止自动恢复，改为手动登录后手动重启批量。

## 6. 前台弹出体验优化

- 需要人工处理时，会尽量把对应标签页切到前台并聚焦窗口，避免页面开在后台不明显。

## 7. 重试上限与跳过机制

- 新增“每篇失败重试上限”硬保护（当前阈值：2）。
- 达到上限后：
  - 停止该篇自动重试。
  - 自动取消该篇勾选（留给后续手动处理）。
  - 记录“重试已停止”日志。
- 批量会绕过该篇并继续后续下载。
- 绕过后新增固定等待（20 秒）再继续下一篇。

## 8. 手动重试可重置上限

- 点击单篇“重试”按钮时，会清空该篇失败计数。
- 即使该篇此前达到上限，也可重新进入自动重试流程。

## 9. 连续失败熔断

- 新增“连续失败 3 篇则停止本轮批量下载”。
- 触发后会输出专门日志并给出状态提示，避免长时间无效重试。

## 10. 列表操作增强

- 每篇新增“删除”按钮，可单条删除并持久化到存储。

## 11. 链接获取能力增强

- 原有“获取链接”：仅处理 `pdfLink` 为空的条目。
- 新增“重新获取链接”按钮：强制对列表条目重新抓取并覆盖 `pdfLink`，用于应对动态链接失效。

## 12. 运行稳定性与可观测性

- 多处错误日志补充了失败原因、页面类型、处理建议。
- 失败/恢复路径状态提示更明确，便于判断当前所处阶段。

---

如果你希望，我可以再补一份 `FLOW.md`，专门画出“正常下载 / 超时恢复 / 验证页 / 重登页 / 跳过与熔断”的状态机流程图，后续维护会更直观。
